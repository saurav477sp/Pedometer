import 'package:flutter/material.dart';

class Days extends StatelessWidget {
  const Days({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Days'));
  }
}

class Weeks extends StatelessWidget {
  const Weeks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Weeks'));
  }
}

class Months extends StatelessWidget {
  const Months({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Months'));
  }
}
